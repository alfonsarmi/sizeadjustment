namespace Workflow.Specs.Helper
{
    public static class GlassApiHelper
    {
        public static async Task<HttpClient> GetLoggedClient(string url, string tenant, string user, string password)
        {
            var client = new HttpClient();
		}
	}
}
